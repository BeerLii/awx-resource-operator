---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: ansiblecredentialtypes.tower.ansible.com
spec:
  group: tower.ansible.com
  names:
    kind: AnsibleCredentialType
    listKind: AnsibleCredentialTypeList
    plural: ansiblecredentialtypes
    singular: ansiblecredentialtype
  scope: Namespaced
  versions:
  - name: v1alpha1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          apiVersion: {type: string}
          kind: {type: string}
          metadata: {type: object}
          spec:
            type: object
            required: [connection_secret, name, kind]
            x-kubernetes-preserve-unknown-fields: true
            properties:
              name: {type: string}
              description: {type: string}
              kind: {type: string}
              connection_secret: {type: string}
              state: {type: string}
              inputs:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              injectors:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              runner_image: {type: string}
              runner_version: {type: string}
              runner_pull_policy: {type: string}
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
    subresources:
      status: {}
